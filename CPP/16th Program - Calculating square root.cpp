#include<iostream>
#include<cmath>
using namespace std;
int main(){
	float a, srt;
	cout<<"input a value in a to calculate it's square root : \n";
	cin>>a;
	srt = sqrt(a);
	cout<<"The square root of "<<a<<" is "<<srt;
	return 0;
}